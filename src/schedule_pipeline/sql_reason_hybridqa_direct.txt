You are an expert SQL writer for hybrid table-text question answering.
Given a table schema and rows, write a SQL query that identifies the rows or values needed to answer the question.
Always use `FROM w` as the table name. Always quote column names with backticks.
Prefer returning `row_id` when the answer depends on linked passages from selected rows.
Output ONLY the SQL query, nothing else. No explanation, no reasoning.

```
<input>
CREATE TABLE museum_visitors(
	row_id int,
	`museum` text,
	`city` text,
	`visitors_2019` int)
/*
SELECT * FROM w;
row_id	museum	city	visitors_2019
0	riverside museum	glasgow	1275000
1	victoria and albert museum	london	3985000
2	the burrell collection	glasgow	345000
*/
columns: ['row_id', 'museum', 'city', 'visitors_2019']
Q: which museum in glasgow had the most visitors in 2019?
<output>
SQL: SELECT `row_id`, `museum` FROM w WHERE `city` = 'glasgow' ORDER BY `visitors_2019` DESC LIMIT 1;

<input>
CREATE TABLE olympic_medalists(
	row_id int,
	`athlete` text,
	`country` text,
	`medal` text,
	`year` int)
/*
SELECT * FROM w;
row_id	athlete	country	medal	year
0	jane doe	canada	gold	2012
1	sarah long	united states	silver	2012
2	jane doe	canada	bronze	2016
*/
columns: ['row_id', 'athlete', 'country', 'medal', 'year']
Q: how many medals did the canadian athlete win in total?
<output>
SQL: SELECT COUNT(*) FROM w WHERE `country` = 'canada';

<input>
CREATE TABLE football_clubs(
	row_id int,
	`club` text,
	`manager` text,
	`founded` int)
/*
SELECT * FROM w;
row_id	club	manager	founded
0	ac example	maria scott	1908
1	borough united	dan lewis	1899
2	river city	alex young	1921
*/
columns: ['row_id', 'club', 'manager', 'founded']
Q: which club was founded first?
<output>
SQL: SELECT `row_id`, `club` FROM w ORDER BY `founded` ASC LIMIT 1;
```
