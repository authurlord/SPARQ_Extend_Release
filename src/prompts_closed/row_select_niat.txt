You are given a table with potentially nested/hierarchical structure and a question.
Your task is to write an SQLite program to select relevant rows to help answer the question.
The SQLite program need not answer the question directly.
Try to use fuzzy-match for values if you are not sure about the exact values.

Process:
1. Decompose: Find all the subtasks from the main question.
2. Retrieval: Generate an SQLite program for each subtask and combine using an 'OR' statement.

Response Format:
Your response must strictly have the following components:
 - Decompose: Divide the question into smaller manageable sub-tasks; one task for each condition
 - Retrieval: A basic SQLite program for each condition followed by a combined SQLite
**NOTE**
- Strictly use the 'OR' statement when you combine multiple conditions in the final SQLite program.
```
<input>
CREATE TABLE Employee Labor Contracts(
	row_id int,
	group text,
	union text,
	class_or_craft text,
	employees int,
	contract_date text)
/*
3 example rows:
SELECT * FROM w LIMIT 3;
row_id	group	union	class_or_craft	employees	contract_date
0	Mainline	Allied Pilots Association	Pilots	13200	2020
1	Mainline	APFA	Flight Attendants	24900	2019
2	Envoy	ALPA	Pilots	2200	2024
*/
columns: ['row_id', 'group', 'union', 'class_or_craft', 'employees', 'contract_date']
Q: How many Ground School Instructors are represented by TWU?
<output>
Take a deep breath and work step by step
1. Decompose: 
  - Part 1: Find rows where class_or_craft is Ground School Instructors
    Response 1: SELECT * FROM w WHERE `class_or_craft` LIKE '%Ground School Instructors%';
  - Part 2: Find rows where union is TWU
    Response 2: SELECT * FROM w WHERE `union` LIKE '%TWU%';
2. Retrieval:
   # Use 'OR' to combine the conditions from #1, #2
   SQL: SELECT * FROM w WHERE `class_or_craft` LIKE '%Ground School Instructors%' OR `union` LIKE '%TWU%';

<input>
CREATE TABLE LNG Plants(
	row_id int,
	plant_name text,
	country text,
	startup_date text,
	capacity text)
/*
3 example rows:
SELECT * FROM w LIMIT 3;
row_id	plant_name	country	startup_date	capacity
0	Qatargas II	Qatar	2009	7.8
1	Arzew GL4Z	Algeria	1964	0.90
2	Nigeria LNG	Nigeria	1999	23.5
*/
columns: ['row_id', 'plant_name', 'country', 'startup_date', 'capacity']
Q: How much capacity does the Sakhalin II plant have?
<output>
Take a deep breath and think step by step
1. Decompose:
 - Part 1: Find rows where plant_name contains Sakhalin
   Response 1: SELECT * FROM w WHERE `plant_name` LIKE '%Sakhalin%';
2. Retrieval:
   # Use the single condition
   SQL: SELECT * FROM w WHERE `plant_name` LIKE '%Sakhalin%';

<input>
CREATE TABLE Eurovision 1992(
	row_id int,
	draw int,
	country text,
	artist text,
	song text,
	place int,
	points int)
/*
3 example rows:
SELECT * FROM w LIMIT 3;
row_id	draw	country	artist	song	place	points
0	1	Yugoslavia	Extra Nena	Ljubim te pesmama	13	44
1	2	France	Kali	Monté la rivié	8	73
2	3	Denmark	Lotte Nilsson	Alt det som ingen ser	12	47
*/
columns: ['row_id', 'draw', 'country', 'artist', 'song', 'place', 'points']
Q: Which country finished in position 12?
<output>
Take a deep breath and work step by step
1. Decompose:
  - Part 1: Find rows where place is 12
    Response 1: SELECT * FROM w WHERE `place` = 12;
2. Retrieval:
   # Use the single condition
   SQL: SELECT * FROM w WHERE `place` = 12;
```
